import type {CapacitorConfig} from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ir.surveying.demo',
  appName: 'سامانه نقشه‌برداری — نسخه آزمایشی',
  webDir: 'dist',
};

export default config;
